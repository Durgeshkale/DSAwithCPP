#include<bits/stdc++.h>
using namespace std;

void explainExtra(){

    int a,n;
    vector<int> v ;

    //sorting
    sort(a, a+n);

    sort(v.begin(), v.end());

    sort(a+2,a+4);

    //sort(a, a+n, greater<int>); //sort in descending order and the greater <int> is the built in comparator


    pair<int,int> a[]={{1,2},{2,1},{4,1}};

    // sort it according to the second element
    //if second element is same , then sort 
    //it according to first element but in descending 

    

}

int main(){
}